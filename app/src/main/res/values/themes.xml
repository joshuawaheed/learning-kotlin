<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyShopApp" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyShopApp" parent="Base.Theme.MyShopApp">
        <item name="titleTextColor">@color/colorWhite</item>
    </style>

    <style name="Theme.MyShopApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MyShopApp.BottomNavigationView.ActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/colorBlack</item>
    </style>

    <style name="Theme.MyShopApp.NoActionBar" parent="Theme.MyShopApp">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MyShopApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>